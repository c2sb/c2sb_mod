-- NOTE: Camera refers to the screen. It is generally difficult or impossible to implement most
-- of these in Starbound. In some cases we don't WANT to implement them because Creatures agents
-- have too much power over the user's interfaces which could have undesirable effects.

--------------------
-- CAOS FUNCTIONS --
--------------------

-- Refreshes the main view port.
function frsh()
end

-- Toggle full screen mode.
-- Returns 1 if in full screen mode, or 0 if in windowed mode.
-- NOTE we shouldn't touch this as it's not relevant.
function wdow()
  return 1
end
